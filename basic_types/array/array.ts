const superHeros: string[] = [];
// const herPower: number[] = [];
const heroPower: Array<number> = []; // define also using that

type UserDetail = {
  name: string;
  isActive: boolean;
};

//array inside array type declartion
const MLModels: number[][] = [[2, 3, 3]];

const allUsers: UserDetail[] = [];

superHeros.push("Fdsfds");
heroPower.push(767);
allUsers.push({ name: "", isActive: true });
